# Dummy credentials for test environment

# These are not real, they just prevent the Supabase client from throwing
# during module initialization when utility tests import store files
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
